# Public Assets Directory

Place your custom images and static files here.

Any files added to this directory will be served directly at the root path during development and copied to the root of the output directory during build.

### Examples:
- Place `hero.jpg` here -> reference it as `/hero.jpg` in your code.
- If you have `Zanzano.jpeg`, place it here -> reference it as `/Zanzano.jpeg`.
